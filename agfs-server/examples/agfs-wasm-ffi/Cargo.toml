[package]
name = "agfs-wasm-ffi"
version = "1.4.0"
edition = "2021"
authors = ["AGFS Contributors"]
description = "Safe Rust SDK for building AGFS WASM plugins"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["rlib"]
